version: '3.8'

services:
  # 1. Nginx (웹 서버) - 최전방 수비수
  nginx:
    image: nginx:1.25
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    depends_on:
      - blog-app
    restart: always

  # 2. Certbot (SSL 인증서 발급 도우미)
  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot

  # 3. Spring Boot 앱 (외부 포트 노출 X)
  blog-app:
    container_name: blog-app
    build: .
    # ports 섹션 제거 (Nginx가 내부망으로 연결함)
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:mysql://blog-db:3306/blog_prod?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - CLOUD_AWS_CREDENTIALS_ACCESS_KEY=${AWS_ACCESS_KEY}
      - CLOUD_AWS_CREDENTIALS_SECRET_KEY=${AWS_SECRET_KEY}
      - BLOG_GIT_WEBHOOK_SECRET=${WEBHOOK_SECRET}
    depends_on:
      - blog-db
    restart: always

  # 4. DB
  blog-db:
    container_name: blog-db
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: blog_prod
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      TZ: Asia/Seoul
    volumes:
      - ./db_data:/var/lib/mysql
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    restart: always
